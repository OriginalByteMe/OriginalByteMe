import { ContactCard } from "noah-portfolio";

// ContactCard — email / GitHub / LinkedIn / blog as interactive matte cards.
// Bind statePath to "/corpus/contact".

export const Default = () => <ContactCard props={{ statePath: "/corpus/contact" }} />;
