import { SkillCloud } from "noah-portfolio";

// SkillCloud — a compact flat cloud of skill pills (logo + name), flattened
// across every category. Bind statePath to "/corpus/skills".

export const Default = () => <SkillCloud props={{ statePath: "/corpus/skills" }} />;
